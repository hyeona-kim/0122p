package com.ict.project.service;

import java.util.List;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;

import com.ict.project.mapper.ChatMapper;
// import com.ict.project.vo.ChatVO;
import com.ict.project.vo.CourseVO;

@Service
public class ChatService1 {
    // @Autowired
    // private ChatMapper ch_mapper;

    // public ChatVO[] chat(String c_idx) {
    // ChatVO[] ar = null;
    // List<ChatVO> list = ch_mapper.chat(c_idx);
    // if (list != null && list.size() > 0) {
    // ar = new ChatVO[list.size()];
    // list.toArray(ar);
    // }
    // return ar;
    // }

    // public int add(ChatVO vo) {
    // return ch_mapper.add(vo);
    // }
}
